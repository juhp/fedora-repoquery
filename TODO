- default mirror config (setup automatically from redirect?)
- use libcurl instead of http-directory (http-directory-curl)
- cache compose file locally
* Centos+EPEL
- centos-stream channels
- read COMPOSE_ID

- types for DIR and FILE urls to enforce trailing slash
